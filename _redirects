# Cloudflare Pages redirects (effective post-DNS-cutover)
# Format: <source> <destination> <status>

# Promo consolidation — collapse parallel offer pages onto /pricing
/intro-offer.html         /pricing.html       301
/holiday-sales-2025.html  /pricing.html       301

# Webflow artifacts — never linked anywhere
/static-template-slug-1748802036035.html  /  301
/style-guide.html                          /  301

# SEO target URLs that don't have dedicated pages yet — route to schedule
# (302 because we plan to build these as money pages in Phase 2)
/salsa-classes-broward         /schedule.html              302
/bachata-classes-davie         /schedule.html              302
/ballroom-dance-classes-davie  /schedule.html              302

# Alternative wedding-dance URL (planned SEO-friendly variant)
/wedding-dance-lessons-broward /wedding-dance-lessons.html 301

# Pretty URLs (no .html extension) are handled automatically by Cloudflare Pages.
# Do NOT add /foo → /foo.html rules here — Cloudflare also auto-redirects
# /foo.html → /foo, which would create an infinite redirect loop.
